name: gym
root: ./
startup_window: connection
windows:
  - connection:
      layout: tiled
      panes:
        - roslaunch airsim_ros_pkgs airsim_node.launch output:=screen host:=$UE4_IP

  

                